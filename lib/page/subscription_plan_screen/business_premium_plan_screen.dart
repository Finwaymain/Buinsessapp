import 'package:flutter/material.dart';
import 'subscription_plan_screen.dart';

class BusinessPremiumPlanScreen extends StatelessWidget {
  const BusinessPremiumPlanScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SubscriptionPlanScreen(
      isbackButton: true,
    );
  }
}
